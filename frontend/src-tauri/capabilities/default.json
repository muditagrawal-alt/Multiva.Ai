{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The studio interface is served by the local engine, so the grant names that origin. Only the folder picker is exposed.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "dialog:allow-open"
  ],
  "remote": {
    "urls": [
      "http://127.0.0.1:*",
      "http://localhost:*"
    ]
  }
}
